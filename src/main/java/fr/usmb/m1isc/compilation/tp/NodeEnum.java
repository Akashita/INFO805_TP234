package fr.usmb.m1isc.compilation.tp;

public enum NodeEnum {
    // Enumération des noeuds
    LET,
    WHILE, DO,
    IF,THEN, ELSE,
    NOT, AND, OR,
    EQUAL,
    INF, SUP,
    INF_EQ, SUP_EQ,
    PLUS, MINUS, MULTI, DIVIDE, MOD, MINUS_UNIT,
    OUTPUT, INPUT, NIL,
    INTEGER, BOOLEAN, IDENTIFIER,
    SEMI
}

